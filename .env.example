# ============================================================================
#  SwiftDocs — frontend environment
#  Copy to .env.local and fill in real values:  cp .env.example .env.local
# ============================================================================

# URL of the deployed backend (Hugging Face Spaces).
# Example: https://novuslabs-swiftdocs-api.hf.space
# For local dev, keep http://localhost:8000
NEXT_PUBLIC_API_URL=http://localhost:8000
